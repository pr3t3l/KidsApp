# Shared contracts

These JSON Schemas are the public boundary between the family web application and the AI service. The server validates every generated proposal before it can be shown or applied. Generated text is never treated as an authorization or safety decision.
